<div class="row col-md-4 col-md-offset-4">
<% if(logged_in?) && (admin?)%>
    <div class="alert alert-danger" role="alert">
        <p>Admins can only add new staff members!</p>
    </div>
<% else %>
    <h1>New user</h1>
    <div class="alert alert-info" role="alert">
        <p>The email address for your account will be your [studentid]@umail.ucc.ie</p>
    </div>
<% end %>

<%= render 'form' %>

