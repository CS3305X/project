<%= form_for(@usertype) do |f| %>
  <% if @usertype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usertype.errors.count, "error") %> prohibited this usertype from being saved:</h2>

      <ul>
      <% @usertype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_id %><br>
    <%= f.number_field :type_id %>
  </div>
  <div class="field">
    <%= f.label :type_name# %><br>
    <%= f.text_field :type_name# %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
